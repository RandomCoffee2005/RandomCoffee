services:
  backend:
    build:
      context: ./randomcoffee
      dockerfile: ../docker/Backend
    volumes:
      - db-vol:/app/db/:rw
    ports:
      - 8080:8080
    env_file: .env

volumes:
  db-vol:
